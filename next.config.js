/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['platform-lookaside.fbsbx.com', 'mosaic.scdn.co', 'images-ak.spotifycdn.com', 'i.scdn.co'],
    }
}

module.exports = nextConfig
